# Deps para CI (Django check + suíte). docxtpl fica fora (só o Docker gera .docx via template).
# weasyprint ENTRA: sem um backend de PDF, os 3 testes de geração de proposta viravam skip
# silencioso e o caminho de PDF não era exercitado por ninguém. Precisa das libs de pango
# (ver o passo "Instalar libs de sistema do WeasyPrint" em .github/workflows/ci.yml).
Django==5.2.16
djangorestframework==3.16
django-tenants>=3.5,<4.0
django-environ==0.12
django-encrypted-model-fields==0.6.5
django-filter==24.3
drf-spectacular==0.28
psycopg2-binary==2.9.10
celery==5.5
django-celery-beat==2.8
requests==2.33.0
argon2-cffi==23.1
django-axes==7.0.1
python-docx>=1.1
# Mesmo range de base.txt — o lock é quem fixa a versão exata (69.0 nos dois).
weasyprint>=62.0
# static files: WhiteNoiseMiddleware é carregado em settings/base.py (todos os ambientes)
whitenoise>=6.6
