TEST_CMD: bash .orch-test.sh

1) Comando(s) de teste rodados + resumo da saída
- `POSTGRES_HOST=localhost POSTGRES_PORT=5436 POSTGRES_USER=sq POSTGRES_PASSWORD=sq POSTGRES_DB=smartquotation python manage.py test apps.accounts.tests.TenantMembersViewTests -v 2 --noinput`
  Resumo: 2 testes executados em `apps.accounts`; `test_get_members_as_admin_returns_200_with_tenant_members` e `test_get_members_as_non_admin_returns_403` passaram (`OK`), validando a tela `/members/` e o RBAC admin-only.
- `bash .orch-test.sh`
  Resumo: `manage.py check` OK; `makemigrations --check --dry-run` OK; suíte Django dos apps do sprint executou 243 testes em 707.574s com status final `OK`.

2) PASS/FAIL de cada comando
- `POSTGRES_HOST=localhost POSTGRES_PORT=5436 POSTGRES_USER=sq POSTGRES_PASSWORD=sq POSTGRES_DB=smartquotation python manage.py test apps.accounts.tests.TenantMembersViewTests -v 2 --noinput`
  PASS
- `bash .orch-test.sh`
  PASS

3) Resumo do diff
- `backend/apps/accounts/tests.py`: adiciona `TenantMembersViewTests` cobrindo `GET /members/` com `200` para admin e `403` para não-admin, além de validar nomes, papéis, status e CREA dos membros do tenant.
- `backend/apps/accounts/views.py`: adiciona `members_view` protegida por `require_role(UserProfile.ROLE_ADMIN)`, com consulta apenas a `UserProfile` do schema corrente e filtro opcional por busca.
- `backend/apps/accounts/urls.py`: registra a rota `/members/`.
- `backend/apps/accounts/templates/accounts/members.html`: cria a página full com Design System G, busca HTMX e acordeão em Alpine.
- `backend/apps/accounts/templates/accounts/_members_table.html`: cria o parcial HTMX com a tabela de membros mostrando contato, papel, status e CREA.

4) Conclusão
- Tarefa concluída porque a tela de membros do tenant foi implementada no app `accounts`, restrita a admin do tenant, listando apenas `UserProfile` do schema corrente, e o gate `bash .orch-test.sh` terminou verde.
