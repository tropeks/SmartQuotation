TEST_CMD: POSTGRES_PORT=5436 bash .orch-test.sh

1) Comandos de teste rodados + resumo da saída
- `POSTGRES_PORT=5436 python backend/manage.py test apps.integrations.nomus -v 2 --noinput`
  Resumo: criou o banco de teste, aplicou migrations incluindo `integrations_nomus.0001_initial`, executou 9 testes de `apps.integrations.nomus` e todos passaram.
- `POSTGRES_PORT=5436 bash .orch-test.sh`
  Resumo: `manage.py check` passou sem issues; `manage.py makemigrations --check --dry-run` retornou `No changes detected`; a suíte do harness executou 199 testes e terminou com `OK`.

2) PASS/FAIL de cada comando
- `POSTGRES_PORT=5436 python backend/manage.py test apps.integrations.nomus -v 2 --noinput` => PASS
- `POSTGRES_PORT=5436 bash .orch-test.sh` => PASS

3) Resumo do diff
- `backend/apps/integrations/nomus/models.py`: adiciona `NomusIntegrationConfig`, `NomusSyncRun` e `NomusExportLog` com segredos criptografados, status/choices e log de conflito/retry.
- `backend/apps/integrations/nomus/migrations/0001_initial.py`: migration inicial do app `integrations_nomus`.
- `backend/apps/integrations/nomus/tests.py`: adiciona testes `TenantTestCase` para criptografia em repouso, `NomusExportLog` com conflito e selector de ERP ativo.
- `backend/apps/integrations/routing.py`: adiciona `get_active_erp()` para resolver `nomus | sap_b1 | none` por tenant.
- `backend/smartquotation/settings/base.py`: registra `apps.integrations.nomus.apps.NomusConfig` em `TENANT_APPS`.

4) Conclusão
- Tarefa concluída porque os modelos e a migration de `nomus` foram criados, o selector de ERP ativo por tenant foi implementado sem quebrar `sap_b1`, os testes novos passaram e o gate `bash .orch-test.sh` ficou verde.
