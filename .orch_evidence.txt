EPICO 2, task 5/6 — painel de log de export Nomus na UI + reexport manual

## Comandos rodados

1) TDD red phase (antes da implementação — views.py/urls.py de nomus ainda não existiam):
   cd backend && .venv/bin/python manage.py test apps.integrations.nomus.tests.NomusExportPanelViewTests -v 2 --noinput
   Resultado: Ran 3 tests ... FAILED (failures=3). Os 3 testes novos batiam 404 (rotas
   'nomus:export_panel' / 'nomus:reexport' inexistentes), confirmando que cobrem
   comportamento ainda não implementado.

2) TDD green phase (depois de views.py + urls.py + template + include no urlconf raiz):
   cd backend && .venv/bin/python manage.py test apps.integrations.nomus.tests.NomusExportPanelViewTests -v 2 --noinput --keepdb
   Resultado: Ran 3 tests in 16.3s — OK.

   Nota de processo: a primeira versão do parcial usava um comentário multi-linha citando o
   próprio texto do GOTCHA ("... só com {% comment %}/{% endcomment %} ...") dentro de um bloco
   {% comment %}...{% endcomment %}. O parser do Django tokeniza o arquivo inteiro antes de
   interpretar tags, então o {% endcomment %} citado como texto dentro do comentário foi
   consumido como o fechamento real, sobrando um {% endcomment %} solto -> TemplateSyntaxError
   "Invalid block tag on line 4: 'endcomment'". Corrigido removendo o bloco {% comment %} e
   deixando um comentário de uma linha com {# ... #} (sem sintaxe de tag citada dentro).
   Reproduz na prática o gotcha citado na tarefa.

3) Regressão da suíte de integração Nomus + produção (sem mocks pulados):
   cd backend && .venv/bin/python manage.py test apps.integrations.nomus apps.production -v 1 --noinput --keepdb
   Resultado: Ran 79 tests in 215.0s — OK.

4) Gate oficial da tarefa:
   bash .orch-test.sh
   Resultado:
   [1/3] Django system check -> OK (0 issues)
   [2/3] makemigrations --check --dry-run -> "No changes detected" (nenhuma migration pendente
        — não criei/alterei nenhum model)
   [3/3] manage.py test apps.quotations apps.proposals apps.production apps.audit apps.accounts
        -> Ran 202 tests in 475.6s — OK
   "=== ALL TESTS PASSED ✓ ==="

TEST_CMD: bash .orch-test.sh

## PASS/FAIL

- Django check: PASS
- makemigrations --check: PASS (sem migrations pendentes — não toquei em models)
- Suite dedicada apps.integrations.nomus.tests.NomusExportPanelViewTests (3 testes novos): PASS
- Suite apps.integrations.nomus + apps.production (79 testes, sem regressão): PASS
- Gate oficial `.orch-test.sh` (202 testes, apps.quotations/proposals/production/audit/accounts): PASS
- pricing_engine: intocado (nenhum arquivo sob pricing_engine/ no diff); gates do motor
  (validate_feixe_completo / validate_permutador_completo) não tocados nem invocados por esta
  tarefa, escopo é backend Django.

## Resumo do diff

Arquivos tocados (nenhum em pricing_engine):

1) backend/apps/integrations/nomus/views.py (NOVO)
   - `_sync_runs_for_of(of)`: filtra NomusSyncRun por entity_type=production_order e
     payload__source_snapshot__of_id=of.pk (correlação OF <-> runs via o campo já gravado por
     serialize_production_order_from_of na task 2/3), com select_related("export_log") para
     evitar N+1 no template.
   - `export_panel(request, of_pk)`: view GET, @login_required (leitura liberada a qualquer
     membro autenticado do tenant), renderiza o parcial "nomus/_export_panel.html" com os
     sync runs + export logs da OF e a flag can_reexport (calculada via
     apps.accounts.rbac.user_role).
   - `reexport(request, of_pk)`: view POST, @require_role(engenheiro, gestor_comercial, admin)
     + @require_POST — chama apps.production.services.reexport_nomus (já existente da task 4)
     e re-renderiza o mesmo parcial (HTMX re-swap do painel), permitindo reexport sem reload de
     página. Usuário autenticado sem um desses papéis recebe 403 (PermissionDenied do próprio
     decorator require_role).

2) backend/apps/integrations/nomus/urls.py (NOVO)
   - app_name="nomus"; rotas
     ofs/<int:of_pk>/nomus/painel/ -> export_panel (name="export_panel")
     ofs/<int:of_pk>/nomus/reexport/ -> reexport (name="reexport")

3) backend/apps/integrations/nomus/templates/nomus/_export_panel.html (NOVO)
   - Tabela Design System G (classe g-table) com Status/Remote ID/Conflito(+motivo)/
     Tentativas/Data por sync run; motivo de conflito em destaque com var(--g-orange);
     estado vazio ("Nenhum export Nomus registrado para esta OF."); form de reexport (só
     renderizado quando can_reexport) com hx-post + hx-target="#nomus-export-panel" +
     hx-swap="innerHTML" para re-swap HTMX puro, sem SPA.

4) backend/apps/production/templates/production/detail.html
   - Nova seção "Exportação Nomus" (g-section), visível apenas quando a OF já foi liberada
     (status liberada/em_producao/concluida — antes disso não há o que exportar), com
     div#nomus-export-panel carregando o parcial via hx-get + hx-trigger="load" (mesmo padrão
     já usado no convertibility-panel de quotations/detail.html).

5) backend/smartquotation/urls.py
   - include("apps.integrations.nomus.urls") no urlconf de tenant, ao lado dos demais apps.

6) backend/apps/integrations/nomus/tests.py
   - Nova classe NomusExportPanelViewTests(TenantTestCase): setUp cria customer/quotation
     aprovada, OF liberada com NomusIntegrationConfig inicialmente desabilitado (evita run
     automático de liberação poluir a contagem), habilita o config depois, e cria 2 sync runs
     manuais via services.maybe_enqueue_export + process_sync_run (2º em conflito, mesmo padrão
     do teste test_reexport_of_already_exported_marks_conflict já existente) para ter dado real
     de conflito a exibir.
     a) test_painel_get_mostra_status_export_e_conflito: GET do painel por usuário orcamentista
        (papel de leitura) retorna 200 e o HTML contém "Success" e o texto exato do
        conflict_reason gravado pelo service.
     b) test_reexport_por_papel_autorizado_cria_novo_run: POST por usuário engenheiro (mock de
        enqueue_sync_run_async para não depender de Celery) retorna 200, cria +1 NomusSyncRun e
        chama o enqueue uma vez.
     c) test_reexport_por_papel_sem_permissao_retorna_403: POST pelo mesmo usuário orcamentista
        (papel sem permissão de escrita para este recurso) retorna 403 e não cria nenhum
        NomusSyncRun novo.

## Conclusão

Tarefa concluída: o log de export/sync Nomus (status, remote_id, conflito+motivo, tentativas,
data) ficou visível na UI da OF via painel HTMX (GET, liberado a qualquer membro do tenant) e
o reexport manual foi exposto como botão que chama o service já existente da task 4
(production.services.reexport_nomus), restrito por RBAC aos papéis de escrita
(engenheiro/gestor_comercial/admin) — usuário sem esses papéis recebe 403. Implementado em TDD
(3 testes escritos e vermelhos antes, verdes depois), sem regressão na suíte de nomus/production
(79 testes) nem no gate oficial do sprint (202 testes), sem tocar em pricing_engine nem em
migrations.
